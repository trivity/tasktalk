services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tasktalk
    ports: ["5432:5432"]
    volumes: ["pg-data:/var/lib/postgresql/data"]
volumes:
  pg-data:
